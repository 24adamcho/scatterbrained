import { Button } from "react-bootstrap";

const NewEdgeReverseAnimation = ({
    
}) => {
    const onClick = () => {
        
    }

    return (
        <Button onClick={()=>{onClick()}}>Reverse animation</Button>
    )
}

export default NewEdgeReverseAnimation;
